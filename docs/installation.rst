============
Installation
============

At the command line::

    $ easy_install assembler_compare

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv assembler_compare
    $ pip install assembler_compare
